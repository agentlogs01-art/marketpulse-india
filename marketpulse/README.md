# MarketPulse package

Application code for MarketPulse India lives in this directory.

Architecture, environment variables, scheduling, and operations are
documented in the repository [README.md](../README.md) (PRD v1.7). Do not
duplicate that guide here — the two copies drifted (version 1.6 vs 1.7,
pytest vs unittest, OpenAI keys the LLM client does not read).
